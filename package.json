{
    "devDependencies": {
        "@babel/preset-env": "^7.27.2",
        "jest": "^30.0.0"
    },
    "scripts": {
        "test": "jest"
    },
    "jest": {
        "transform": {
            "^.+\\.js$": "babel-jest"
        }
    },
    "type": "module",
    "babel": {
        "presets": [
            "@babel/preset-env"
        ]
    }
}